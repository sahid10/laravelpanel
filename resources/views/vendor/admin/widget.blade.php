<div class="card">
    <div class="card-body">
        <h4><span class="{{ $icon ?? 'fas fa-users' }}"></span> {{ $title ?? ''}}</h4>
        <h2 class="card-title">{{ $data ?? 0 }}</h2>
    </div>
</div>